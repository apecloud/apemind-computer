# HomePage Lowest priority
# Do not alias a file from `location = /` — nginx treats that as a
# directory lookup and returns 500.
location = / {
    return 302 /index.html;
}

location = /index.html {
    default_type text/html;
    root /opt/aio;
}
